{
  "name": "ticketing-dapp",
  "version": "1.0.0",
  "description": "Blockchain-based event ticketing system",
  "type": "module",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy": "hardhat run scripts/deploy.ts --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^4.0.6",
    "@nomicfoundation/hardhat-node-test-runner": "^3.0.12",
    "@types/node": "^22.19.15",
    "ethers": "^6.16.0",
    "hardhat": "^3.2.0",
    "typescript": "~5.8.0"
  }
}
